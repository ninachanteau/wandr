<% unless params[:action] == "home"  %>
 <% if user_signed_in? || params[:token].present? %>
  <div class="sidebar-container">
    <div class="sidebar-item">

      <%= link_to root_path, class: "sidebar-wagon-brand" do %>
        <%= image_tag "logo.png", height: 50 %>
      <% end %>
    </div>

    <%# unless params[:action] == "index"  %>
          <%# if @trip && @trip.id.present? %>
      <ul class="list-icons nav nav-tabs" id="myTab" role="tablist">
          <% if @trip.id %>
            <li class="nav-item"> <%= link_to trip_path(@trip), class: "nav-link active", remote: true do %>
              <%= render 'shared/globe-icon' %>
            <% end %></li>
          <% end %>



        <% unless (params[:controller] == "trips" && params[:action] == "index") || (params[:controller] == "pages" && params[:action] == "home") || params[:controller] == "devise_invitable/registrations" %>
          <% if @trip %>
            <li class="nav-item <%= 'jaune' if params[:data] == 'transportation' %>" > <%= link_to trip_transportations_path(@trip), data: "transportation", remote: true do %>
              <i class="fa fa-plane"></i>
            <% end %>
            <% if current_user.new_count(@trip, Transportation, session[:notifications]) != 0 %>
              <div id="transportations-notif" class="notification-holder">
                <div class="notification notification-position text-center"
              data-user-id="<%= current_user.id %>">
                <%= current_user.new_count(@trip, Transportation, session[:notifications]) %>
                </div>
              </div>
            <% end %>
            </li>
            <li class="nav-item"> <%= link_to trip_accommodations_path(@trip, :data => "home"), remote: true do %>
              <i class="fa fa-home <%= 'jaune' if params[:data] == 'home' %>"></i>
            <% end %>
            <% if current_user.new_count(@trip, Accommodation, session[:notifications]) != 0 %>
              <div id="accommodations-notif" class="notification-holder">
                <div class="notification notification-position text-center"
              data-user-id="<%= current_user.id %>">
                <%= current_user.new_count(@trip, Accommodation, session[:notifications]) %>
                </div>
              </div>
            <% end %>
            </li>

            <li class="nav-item"> <%= link_to trip_restaurants_path(@trip), remote: true do %>
              <i class="fa fa-cutlery"></i>
            <% end %>
            <% if current_user.new_count(@trip, Restaurant, session[:notifications]) != 0 %>
              <div id="restaurants-notif" class="notification-holder">
                <div class="notification notification-position text-center"
              data-user-id="<%= current_user.id %>">
                <%= current_user.new_count(@trip, Restaurant, session[:notifications]) %>
                </div>
              </div>
            <% end %>
            </li>
            <li class="nav-item"> <%= link_to trip_activities_path(@trip), remote: true do %>
              <i class="fa fa-university" style="font-size: 26px"></i>
            <% end %>
            <% if current_user.new_count(@trip, Activity, session[:notifications]) != 0 %>
              <div id="activities-notif" class="notification-holder">
                <div class="notification notification-position text-center"
              data-user-id="<%= current_user.id %>">
                <%= current_user.new_count(@trip, Activity, session[:notifications]) %>
                </div>
              </div>
            <% end %>
            </li>
          <% end %>
        <% end %>
      </ul>
        <% end %>
    <%# end %>

   <!--  <div class="tab-content" id="myTabContent">
      <div class="tab-pane fade show active" id="trips#index" role="tabpanel" aria-labelledby="trips-tab">Trip index</div>
      <div class="tab-pane fade show active" id="trips#show" role="tabpanel" aria-labelledby="trip-tab">Trip show</div>
      <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="transportations-tab">Transportations</div>
      <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">...</div>
    </div> -->
  </div>

<%# end %>
<% end %>
