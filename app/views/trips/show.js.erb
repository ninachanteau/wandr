function loadTrip(innerHTML) {
  const wrapper = document.querySelector('.wrapper');
  wrapper.innerHTML = innerHTML;
};

loadTrip('<%= j render "show", trip: @trip, participation: @participation, events: @events, markers: @markers %>');
