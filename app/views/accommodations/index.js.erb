
function loadAccommodations(innerHTML) {
  const wrapper = document.querySelector('.wrapper');
  wrapper.innerHTML = innerHTML;
};

loadAccommodations('<%= j render "index", trip_dates: @trip_dates, accommodations: @accommodations, trip_participants: @trip_participants, trip: @trip, accommodation: @accommodation, my_accommodations: @my_accommodations %>');
