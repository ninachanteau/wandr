
function loadAccommodations(innerHTML) {
  const wrapper = document.querySelector('.wrapper');
  wrapper.innerHTML = innerHTML;
};

function changeTitle(title) {
  const navbarTitle = document.querySelector('.navbar-title');
  navbarTitle.innerHTML = title;
}

// function returnCard(backCard) {
//   const plusReturn = document.querySelectorAll(".plus-return");
//     plusReturn.forEach((element) => {
//       element.addEventListener("mouseover", (event) => {
//         const selectedCard = event.currentTarget.querySelector("index-card");
//         const hiddenCard = event.currentTarget.querySelector("index-card.hidden");
//           plusReturn.forEach((elem) => {
//             elem.querySelector("index-card.hidden").classList.remove("hidden");
//           });
//         selectedCard.classList.toggle("hidden");
//       });
//     });
// }

changeTitle('<%= j render "shared/navbar_title", trip: @trip, icon: "home" %>');


loadAccommodations('<%= j render "index", accommodations: @accommodations, trip_participants: @trip_participants, trip_dates: @trip_dates, trip: @trip, accommodation: @accommodation, my_accommodations: @my_accommodations %>');

function removeNotification() {
  const notification = document.getElementById('accommodations-notif');
  if (notification) {
    notification.remove();
  }
}

removeNotification()

