<div class="new-container">
  <div class="container">

    <div class="header">
      <div class="title text-center"> <h2><%= @trip.name  %>  ...  <i class="fa fa-home title-icon"></i></h2>
      </div>
    </div>

<h4>New accommodation</h4>

  <div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= simple_form_for [@trip, @accommodation] do |f| %>
          <%= f.input :start_date, as: :string, required: false, input_html: { class: "datepicker" }, class: 'form-control' %>
          <%= f.input :end_date, as: :string, required: false, input_html: { class: "datepicker" }, class: 'form-control' %>
          <%= f.input :name, label: "Name", class: 'form-control' %>
          <%= f.input :url, label: "Link", class: 'form-control' %>
          <!-- Ici on pourrait suggérer l'adresse , tel etc directement en scrapant l'url par AJAX - semaine prochaine -->
          <%= f.input :address, label: "Address", class: 'form-control' %>
          <%= f.input :phone_number, label: "Phone number", class: 'form-control' %>
          <%= f.input :email, label: "Email", class: 'form-control' %>
          <%= f.input :number_of_rooms, label: "# rooms", class: 'form-control' %>
          <%= f.input :number_of_nights, label: "# nights", class: 'form-control' %>
          <%= f.input :total_price, label: "Total price", class: 'form-control' %>
          <%= f.input :status, collection: ["Wishlist", "Booked"], default: "Wishlist", class: 'form-control' %>
          <!-- Add participants -->
          <%= f.submit class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
