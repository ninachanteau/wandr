function loadActivities(innerHTML) {
  const wrapper = document.querySelector('.wrapper');
  wrapper.innerHTML = innerHTML;
};

loadActivities(`'<%= j render "index", trip_dates: @trip_dates, activities: @activities, participation: @participation, trip_participants: @trip_participants, trip: @trip, activity: @activity,  my_activities: @my_activities %>'`);
